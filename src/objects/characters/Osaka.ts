import { Player } from '../Player';

export class Osaka extends Player {
    // Osaka behavior is now handled through character callbacks in CharacterFactory
    // This class is kept for future extensibility or scene-specific logic
}

