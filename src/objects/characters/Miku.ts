import { Player } from '../Player';

export class Miku extends Player {
    // Miku behavior is now handled through character callbacks in CharacterFactory
    // This class is kept for future extensibility or scene-specific logic
}

